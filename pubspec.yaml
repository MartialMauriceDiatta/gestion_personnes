name: Gestion Bus
description: "Application Flutter de gestion de la liste de rappel au depart du bus de Bransan"
publish_to: 'none'

version: 1.0.0+1

# AJOUTE CETTE SECTION OBLIGATOIRE :
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true